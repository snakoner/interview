> RVO/NRVO приоритетнее чем move-семантика

## Return Value Optimization (RVO) и Named Return Value Optimization (NRVO)

Другое название для RVO/NRVO - copy elission (пропуск копий).

Рассмотрим код:

```cpp
class C {
public:
    C() {}
};

C f() {
    C local_variable;
    // Действия с local_variable
    return local_variable;
}

int main() {
    C result = f();
}

```

Можно предположить следующий порядок действий:
1. создание `local_variable` при помощи вызова конструктора по умолчанию класса `C`;
2. вызов конструктора копии класса C,  чтобы копировать `local_variable` в `result`;
3. вызов деструктора для `local_variable`.

Но это в случае. если не будет применено **Named Return Value Optimization**:
1. компилятор создаёт конструктором по умолчанию до вызова функции `f()` переменную `result` ;
2. затем неявно передаёт в функцию `f()` указатель на `result` ;
3. в рамках функции `f()` не создаёт `local_variable`, а вместо этого работает с указателем на `result`;
4. в return ничего не копируется, поскольку данные уже там.


Что же касается **Return Value Optimization**, то это такая же оптимизация, как **NRVO**, но для случаев, когда экземпляр возвращаемого класса создаётся прямо в операторе return:

```cpp
C f() {
    return C();
}
```

Обычно компилятор, даже со всеми включёнными оптимизациями, не обязан применять RVO/NRVO, а лишь имеет на это право. Поговорим о том, что мешает компилятору применять эти оптимизации, а что помогает, и о том, как повышать шансы на их применение.



