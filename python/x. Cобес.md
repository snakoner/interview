### Easy

1.	Какие типы данных есть в Python?
- Числовые: int, float, complex
- Логические: bool
- Последовательности: str, list, tuple, range
- Множества: set, frozenset (неизменяемое множество)
- Отображения: dict
- Бинарные: bytes – неизменяемые последовательности байтов, bytearray – изменяемые последовательности байтов
- None – специальное значение, представляющее “ничто”

2.	В чём разница между list и tuple?
> list - изменяемый, tuple - неизменяемый

3.	Как работают is и ==?
> == (оператор равенства): проверяет значение двух объектов, т.е. сравнивает их содержимое.
> is (оператор идентичности): оператор is проверяет идентичность объектов в памяти (то есть, указывают ли переменные на один и тот же объект).
```python
x = [1, 2, 3]
y = x  # y ссылается на тот же объект, что и x

print(x is y)  # True, так как обе переменные указывают на один и тот же объект в памяти
```

4.	Что такое None?
> None – специальное значение, представляющее “ничто”

5.	Какой результат у bool([]), bool(''), bool(0)?
> None

6.	Как работает range()?

> range() создаёт не список, а объект диапазона (итерируемый объект, который вычисляет значения по мере необходимости). Быстрее, чем создавать список в памяти

7.	Что делает zip()?

> Объединение нескольких итерируемых объектов:
```python
a = [1, 2, 3]
b = ['a', 'b', 'c']
result = zip(a, b)
print(list(result))  # [(1, 'a'), (2, 'b'), (3, 'c')]
```

8.	Как поменять местами две переменные без temp?

> a, b = b, a

9.	Как передаются аргументы в функции — по значению или по ссылке?

Передается ссылка на объект. Но:
- Неизменяемые типы: int, float, str, tuple, frozenset: При передаче этих типов данных в функцию создаётся ссылка на объект, но так как они не могут быть изменены, любые изменения внутри функции не будут затрагивать оригинальный объект.
- Изменяемые типы: list, dict, set: При передаче этих типов данных в функцию передаётся ссылка на объект, и изменения, сделанные в объекте, отразятся на оригинальном объекте.


10.	Разница между deepcopy() и copy()?

> copy() — поверхностное копирование: Для неизменяемых объектов (например, числа или строки) создается новая ссылка.Для изменяемых объектов (например, вложенных списков или словарей) в новом объекте остаются ссылки на те же вложенные объекты, что и в оригинале. То есть, если вы измените вложенные объекты в копии, это отразится и на оригинале.

> deepcopy() - глубокое копирование: она рекурсивно копирует все вложенные объекты

### Middle

11.	Что такое *args и **kwargs?

> *args позволяет функции принимать неопределённое количество позиционных аргументов. С помощью *args можно передавать любое количество аргументов, и они будут собраны в кортеж.
> **kwargs позволяет функции принимать неопределённое количество именованных (ключевых) аргументов. Они будут собраны в словарь, где ключи — это имена аргументов, а значения — переданные значения.

12.	Как работают замыкания (closures)?
> Замыкания позволяют функции использовать переменные из внешних функций, даже если они уже завершили выполнение
```python
def outer_function(x):
    # Переменная x - это свободная переменная для inner_function
    def inner_function(y):
        return x + y
    return inner_function

closure = outer_function(10)  # outer_function выполняется и возвращает функцию inner_function
print(closure(5))  # 10 + 5 = 15
```

13.	Что делают декораторы @staticmethod, @classmethod и @property?

- @staticmethod используется для создания статического метода в классе. Статический метод не зависит от состояния экземпляра класса и может быть вызван как через сам класс, так и через экземпляр.
- @classmethod используется для создания метода класса. Он принимает класс в качестве первого аргумента, обычно называемого cls, вместо экземпляра (self).
- @property позволяет превращать метод в свойство. Это значит, что метод можно вызывать как атрибут, не используя круглые скобки, и он будет вести себя как обычный атрибут, но с возможностью добавления логики.

- self — это первый параметр метода экземпляра (методы, которые относятся к конкретным объектам класса). Он ссылается на сам объект (экземпляр класса), для которого был вызван метод.

- cls — это первый параметр метода класса (методы, которые относятся к самому классу, а не к его экземплярам). Он ссылается на сам класс, и через него можно работать с атрибутами и методами класса.

```python
class MyClass:
    class_variable = 0  # Атрибут класса

    @classmethod
    def increment_class_variable(cls):
        cls.class_variable += 1  # Доступ к атрибуту класса
        print(cls.class_variable)

# Вызов метода класса
MyClass.increment_class_variable()  # 1
MyClass.increment_class_variable()  # 2
```

14.	Как работает super()?

> Функция super() в Python используется для вызова методов из родительского класса (или класса, который является базовым для текущего класса)

15.	Что такое MRO (Method Resolution Order)?

> MRO помогает избежать проблем, связанных с множественным наследованием
```python
class A:
    def method(self):
        print("Method from A")

class B(A):
    def method(self):
        print("Method from B")

class C(A):
    def method(self):
        print("Method from C")

class D(B, C):
    pass

d = D()
d.method() # Method from B
```

Python использует MRO для поиска метода в следующем порядке: сначала в классе D, затем в классе B, потом в классе C, и в последнюю очередь в классе A.


16.	Разница между isinstance() и type()?

> type возвращает тип класса, к которому относится объект, но не учитывает наследование:
```python
type(int(3)) # <class 'int'>
```

> isinstance(object, classinfo) учитывает наследование

```python
class Animal:
    pass

class Dog(Animal):
    pass

dog = Dog()

print(isinstance(dog, Dog))    # True
print(isinstance(dog, Animal))  # True
```


19.	Как работает dataclass?

### Исключения и Работа с памятью

21.	Как обрабатывать исключения в Python?
22.	Зачем нужен finally в try/except?

> finally выполняется всегда!

23.	Что делает with open("file.txt") as f:?

> Эта конструкция используется в Python для безопасного открытия и работы с файлами. После выхода из области видимости файл автоматически закрывается

24.	Как вручную вызвать исключение?

- raise ValueError("Некорректное значение!")  
- raise Exception("Message")

Можно создать самому:

```python
class MyError(Exception):
    def __init__(self, message):
        super().__init__(message)

# Использование кастомного исключения
x = -5
if x < 0:
    raise MyError("Значение не может быть отрицательным!")
```

25.	Разница между raise и raise e?
- raise  # Передаёт исключение дальше

26.	Что такое garbage collector в Python?

В Python используется автоматическое управление памятью, которое включает:
1.	Подсчёт ссылок (Reference Counting)
2.	Сборку циклических ссылок (Generational Garbage Collection)

Подсчёт ссылок:

Каждый объект в Python имеет счётчик ссылок – сколько переменных на него ссылаются.

Если счётчик становится 0, объект удаляется автоматически.

Python делит объекты на 3 поколения (0, 1, 2):
- Новые объекты → поколение 0
- Долгоживущие объекты перемещаются в 1, потом в 2
- GC чаще очищает 0, реже 1 и 2 (оптимизация)

- Если нет ссылок на объект → удаляется сразу
- Если есть циклические ссылки, но объект не нужен → GC удалит
- GC не трогает глобальные переменные до завершения программы

27.	Как работает del?
- Удаляет объекты, элементы массива, ключи из словаря
- Если удалить все ссылки на объект, он будет удален из памяти (GC – Garbage Collector):
- В Python не всегда удаляется объект сразу – он остаётся в памяти, пока не сработает Garbage Collector (GC).

29.	Как можно освободить память в Python?

del

30.	Что такое weakref?

Обычно, если переменная ссылается на объект, он не удаляется, пока остаётся хотя бы одна ссылка.

Но слабая ссылка (weakref) не увеличивает счётчик ссылок, позволяя GC удалить объект, когда на него больше нет обычных ссылок.

31.	Разница между sys.getrefcount() и gc.collect()?
- gc.collect()  - принудительный запуск GC
- sys.getrefcount(var) - возвращает количество ссылок на объект


### Продвинутые темы (Senior)

31.	Как работает GIL (Global Interpreter Lock)?
32.	Как работает asyncio?
33.	Разница между потоками (threading) и процессами (multiprocessing)?
34.	Что делает yield? Разница между return и yield?
35.	Как работает async/await?
36.	Как работают deque, defaultdict, Counter?
37.	Как ускорить Python-код?
38.	Как работает Cython и PyPy?
39.	Разница между pickle и json?
40.	Что такое metaclass в Python?

