### Типы данных

#### Value types

Такие значения всегда передаются по значению при вызове функций.

- `bool`: `true` / `false`, значение по умолчанию `false`
- `integers`: существует много типов целых чисел:
  - `int` / `uint`
