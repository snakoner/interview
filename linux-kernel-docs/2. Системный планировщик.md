# 2. Системный планировщик

## Типы операционных систем
**Многозадачность** - возможность ОС выполнять параллельно или псевдопараллельно несколько процессов.

**Кооперативная многозадачность** - процесс не прерывается принудительно, процесс сам знает, когда нужно прервать свое выполнение. Плохо, так как планировщик не может управлять процессами, и в теории процесс может монополизировать все процессорное время.

**Вытесняющая многозадачность** - системный планировщик самостоятельно принимает решение о том, в какой момент времени следует прекратить выполнение текущего процесса и переключиться на выполнение другого процесса.

$$ \text{Загруженность процессора} = \frac{t_{\text{non idle tasks}}}{t_{\text{non idle tasks}} + t_{\text{idle task}}} $$

## Планировщик Linux

Обьект записи в красно-черном дереве планировщика - `sched_entity`: 
```cpp
struct sched_entity {
    struct load_weight load;
    struct rb_node run_node;
    struct list_head group_node;
    unsigned int on_rq;
    u64 exec_start;
    u64 sum_exec_runtime;
    u64 vruntime;
    u64 prev_sum_exec_runtime;
    u64 last_wakeup;
    u64 avg_overlap;
    u64 nr_migrations;
    u64 start_runtime;
    u64 avg_wakeup;
    /* Множество переменных stat опущено, они появляются, только
    если установлен параметр конфигурации CONFIG_SCHEDSTATS */
};
```

`sched_entity` встраивается в `task_struct` как поле `se`.

Все процессы можно разделить на процессы реального времени (интерактивные процессы, где время отклика критично, например операции ввода-вывода) и условные процессы (активно использующие CPU). В `sched_entity` может быть несколько политик планирования одновременно.

У каждого типа процессов есть свой алгоритм планирования. При этом пока есть готовые к выполнению процессы реального времени, выполняться будут они, оставляя условные процессы в ожидании.

## Планирование процессов реального времени:
Используются две политики: `SCHED_RR` и `SCHED_FIFO`.

Для процессов реального времени есть `SCHED_FIFO` и `SCHED_RR`, для условных процессов - `SCHED_NORMAL`. Процессы с политикой `SCHED_FIFO` и `SCHED_RR` всегда вытесняют процесс с политикой `SCHED_NORMAL`, то есть имеют приоритет выше.

### SCHED_FIFO
Процессы планируются **без квантов времени** по алгоритму FIFO: первый добавленный процесс будет выполнен первым. Выполняется до тех пор, пока:
1. явный системный вызов `sched_yield` в процессе, после чего он добавляется в конец FIFO.
2. нет более высокоприоритетного процесс с `SCHED_FF`, `SCHED_RR`, иначе она вытесняется
3. иначе может монополизировать процессор

### SCHED_RR
Процессы планируются с квантами времени по циклическому алгоритму. Если у задачи закончился квант времени, она помещается в конец очереди задач с таким же приоритетом, т.е. перепланирование происходит по циклическому алгоритму. Можно сказать, что каждая задача получает справедливое количество процессорного времени.

Выполняется пока:
1. Не исчерпан квант времени
2. есть более высокоприоритетный процесс с `SCHED_FF`, `SCHED_RR`

Для задач реального времени приоритет: [0, MAX_RT_PRIO - 1], где обычно MAX_RT_PRIO = 100.

Функции:
```cpp
sched_setscheduler(); // задать стратегию планирования для процесса
sched_getscheduler(); // узнать стратегию планирования для процесса
sched_setparam(); // задать приоритет для rt-процесса
sched_getparam(); // узнать приоритет для rt-процесса
sched_yield(); // передать управление другим процессам
```

## Планирование условных процессов: SCHED_NORMAL.

Используется `CFS` - complitely fair scheduler.

### Алгоритм планирования


Исходя из `nice` кадому процессу изначально назначается время выполнения:
> $$T_{exec} = T \cdot \frac{W}{W_{total}}$$

$$\text{где } W  - \text{вес процесса}, W_{total} - \text{сумма весов всех процессов}, T - \text{общее процессорное время}$$

Каждому `nice` ставится в соответствие свой вес, наибольший у процесса с `nice = -20`. Сам `nice` лежит в диапазоне [-20:19].

Если в системе есть два процесса с одинаковым приоритетом, то каждому из них будет выделено одинаковое количество процессорного времени вне зависимости от их значения nice.
Разница в процессорном времени у двух процессов с разными nice зависит не от абсолютного значения nice, а от относительного: есть два процесса с nice 0, -5 и процессы с nice 20, 15. Высокоприоритетный процесс -5 получит столько же времени, что и процесс с nice = 15. Аналогично с 0 и 20.

Может случится ситуация, что при большом количестве процессов время, выделяемой какому-то процессу будет слишком малым(издержки на переключение контекста окажутся больше). В Linux есть ограничение(minimal granularity, по умолчанию 1мс), которое указывает на минимально возможное время выполнения процесса.

Для выбора следующего процесса на выполнение используется `vruntime` - Виртуальное время выполнения процесса – это количество времени, потраченного именно на выполнение, без учета любых ожиданий.
CFS выбирает следующую задачу на выполнение по минимальному `vruntime`.
CFS задействует красно-черное дерево, представляющее бинарное дерево поиска – то есть добавление, удаление и поиск выполняются за `O(logN)`. Новые процессы или процесс, возвращающиеся из ожидания в состояние готовности, добавляются в дерево с ключом vruntime = `min_vruntime`.

> Отличие `RR` от `CFS` в том, что в RR квант времени статичен и не зависит от количества процессов в системе. В CFS квант времени может меняться в зависимости от количества процессов в системе. Кроме того, и для RR и для CFS есть виртуальное время выполнения, процессор всегда выбирает из списка(RR) или RB-tree(CFS) процесс с наименьшим виртуальным временем.

## Работа с планировщиком

schedule() запускает системный планировщик. Внутри вызывается функция pick_next_task(), которая обходит все классы планировщика, начиная с самого высокоприоритетного и выбирает самый высокоприоритетный процесс и возвращает его.

## Вытеснение и переключение контекста

Под переключением контекста будем понимать переход от одной выполняемой задачи к другой, который осуществляется с помощью функции `context_switch()`.  Эта функция вызывается из функции `schedule()` после выбора нового процесса, который должен быть запущен. В ней выполняются два важных шага:
1. Вызывается функция `switch_mm()`. Она выполняет переход от виртуальной памяти старого процесса к виртуальной памяти нового процесса.
2. Вызывается функция `switch_to()`, которая переключает состояние процессора от старой задачи к новой. Эта процедура включает сохранение и восстановление информации стека ядра и регистров процессора, а также любого другого состояния, которое зависит от применяемой аппаратной платформы и должно выполняться для каждого процесса.

> При смене задачи происходит очистка TLB (чтобы для последующего использования не было много missов, TLB для определенного процесса сохраняется в памяти процесса)
